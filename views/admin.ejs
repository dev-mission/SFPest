<%- contentFor('head') %>
<base href="/admin/">
<%- contentFor('body') %>
<app-admin>Loading...</app-admin>
<% for (let entry of ['polyfills', 'vendor', 'admin']) { %>
  <% for (let chunk of webpackStats.chunks[entry]) { %>
    <script type="application/javascript" src="<%= chunk.publicPath %>"></script>
  <% } %>
<% } %>
